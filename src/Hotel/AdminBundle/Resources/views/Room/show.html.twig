{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Room</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Numberofpeople</th>
                <td>{{ entity.numberOfPeople }}</td>
            </tr>
            <tr>
                <th>Forsmokingpeople</th>
                <td>{{ entity.forSmokingPeople }}</td>
            </tr>
            <tr>
                <th>Roomnumber</th>
                <td>{{ entity.roomNumber }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="table">
    <li>
        <a href="{{ path('room') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('room_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
