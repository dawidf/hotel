{% extends '::base.html.twig' %}

{% block body -%}
    <h1>News creation</h1>

    {{ form(form) }}

        <ul class="table">
    <li>
        <a href="{{ path('news') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
