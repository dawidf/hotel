{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Floor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Floor</th>
                <td>{{ entity.floor }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="table">
    <li>
        <a href="{{ path('floor') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('floor_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
